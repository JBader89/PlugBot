var api = require('./dictionaryapi');

try {
    var dict = new api.DictionaryAPI(api.LEARNERS, 'YOUR_API_KEY');
    dict.query('apple', function(err, result) {
        console.log(result);
    });
}
catch (e) {
    console.log('[' + e.name + '] ' + e.message);
}
